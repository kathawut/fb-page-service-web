{
  "name": "facebook-pages-api-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js",
    "start:preview": "vite preview --host 0.0.0.0 --port 8080",
    "deploy": "npm run build && firebase deploy",
    "deploy:do": "npm run build"
  },
  "devDependencies": {
    "vite": "^4.5.0"
  }
}
